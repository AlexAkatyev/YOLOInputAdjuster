# YOLOInputAdjuster
Paste the image into the specified window without stretching it, and adjust the YOLO markup. This is necessary to train the model on full-size objects, without scaling.

# YOLOInputSelect640
Select the area with the 640 size marks on the large image.
